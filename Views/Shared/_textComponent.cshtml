@model TextComponent

@if (!string.IsNullOrEmpty(Model.Text))
{
    if (Model.Text.Length > Model.Length)
    {
        <span>
            @Model.Text.Substring(0, Model.Length)
        </span>
    }
    else
    {
        <span>
            @Model.Text
        </span>
    }
}