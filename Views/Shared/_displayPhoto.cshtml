@model byte []

@{

    try
    {
        if (Model != null)
        {
            if (Model.Length > 0)
            {
                var src = $"data:image/png;base64,{Convert.ToBase64String(Model)}";
                <img src="@src" />
            }
        }
    }

    catch { }

}